test:
  adapter: postgresql
  host: localhost
  port: <%= ENV.fetch("POSTGRES_PORT", 5433) %>
  database: rails_solid_queue_0_test_database
  username: <%= ENV.fetch("POSTGRES_USER", "postgres") %>
  password: <%= ENV.fetch("POSTGRES_PASSWORD", "postgres") %>
